<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Dashboard - Open Payments</title>
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <div class="dashboard">
    <header class="dashboard-header">
      <h1>Open Payments Dashboard</h1>
      <div class="user-info">
        <span>Welcome, <%= user.full_name %> (Child Account)</span>
        <a href="/auth/logout" class="btn btn-secondary">Logout</a>
      </div>
    </header>

    <div class="dashboard-content">
      <!-- Wallet Information -->
      <div class="card">
        <h2>Your Wallet</h2>
        <div class="wallet-balance">
          <span class="currency">$</span>
          <span class="amount"><%= wallet.balance.toFixed(2) %></span>
        </div>
        <p class="wallet-id">Wallet ID: <%= wallet.id %></p>
        <% if (parent) { %>
          <p class="text-muted">Managed by: <%= parent.full_name %> (<%= parent.email %>)</p>
        <% } %>
      </div>

      <!-- Transactions Viewer -->
      <div class="card" style="grid-column: 1 / -1;">
        <h2>My Transactions & Balance</h2>
        <div class="form-group">
          <button id="sync-now-btn" class="btn btn-secondary" type="button">Sync Now</button>
        </div>

        <div class="balance-display">
          <strong>Balance:</strong> <span id="user-balance">-</span>
        </div>

        <div class="transactions-grid">
          <div class="transaction-section">
            <h3>📥 Incoming Transactions</h3>
            <table class="transactions-table">
              <thead><tr><th>Type</th><th>Amount</th><th>Status</th><th>Date</th></tr></thead>
              <tbody id="incoming-table-body"><tr><td colspan="4" style="text-align: center; color: #999;">Loading...</td></tr></tbody>
            </table>
          </div>

          <div class="transaction-section">
            <h3>📤 Outgoing Transactions</h3>
            <table class="transactions-table">
              <thead><tr><th>Type</th><th>Amount</th><th>Status</th><th>Date</th></tr></thead>
              <tbody id="outgoing-table-body"><tr><td colspan="4" style="text-align: center; color: #999;">Loading...</td></tr></tbody>
            </table>
          </div>
        </div>
      </div>

      <!-- Information -->
      <div class="card info-card">
        <h3>Account Information</h3>
        <p>As a child account, your parent has full control over transfers in and out of your wallet.</p>
        <p>Your parent can view your balance and transaction history.</p>
      </div>
    </div>
  </div>

  <script>
    // Set current user ID for dashboard.js to use
    window.currentUserId = '<%= user.id %>';
  </script>
  <script src="/js/dashboard.js"></script>
  <script>
    // Auto-load child's own transactions after dashboard.js loads
    if (window.currentUserId && typeof loadUserData === 'function') {
      loadUserData(window.currentUserId);
    }
  </script>
</body>
</html>
